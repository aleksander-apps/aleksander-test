# Wdrożenie

Tor wydań: push na `main` → Jenkins (`torWydan`) → Harbor `www/aleksander-test` → repo `stacki` → Komodo → `/api/health`.
Port 3300 na `www` (CT 151). Sekrety: `stacki/www/aleksander-test/sekrety.env` (SOPS).
