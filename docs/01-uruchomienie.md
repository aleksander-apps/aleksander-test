# Uruchomienie

```bash
pnpm install
pnpm dev            # http://localhost:3300
pnpm test           # jednostkowe (offline)
docker compose -f docker-compose.test.yml run --rm test   # jednostkowe + integracyjne z Postgresem
```
